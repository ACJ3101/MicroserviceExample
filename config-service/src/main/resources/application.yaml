server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin
          uri: https://github.com/ACJ3101/MicroserviceExample
          search-paths: config-data
  application:
    name: config-service