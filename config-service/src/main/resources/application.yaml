server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/ACJ3101/MicroserviceExample
          search-paths: config-data
  application:
    name: config-service